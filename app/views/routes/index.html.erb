<h2> Routes </h2>

<%= link_to "Create New Route", routes_path %>

<%= @routes.each do |route| %>
    <h3><%= link_to route.name, routes_direcitions_path(route) %></h3>
    #<p><%= display total travel time %></p>
<% end %> 