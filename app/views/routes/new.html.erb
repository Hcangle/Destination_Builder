<h2> Build a New Route </h2>

<% @route.errors.full_messages.each do |error| %>
  <p> <i><%= error %></i> </p>
    <% end %>

<%= form_for @route do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit "Create Route" %>
<% end %>
